# tailwindcss-fully-fluid / testing react app

## Usage

```sh
npm install
```

```sh
npm start
# react-scripts by default clear the console so to overcome this I use `concurrently`
npx concurrently npm:start
```
